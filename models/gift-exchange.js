var names = []

export default class GiftExchange{
    pairs(names){
        if(names.length % 2 != 0){
            throw new Error("number of names cannot be odd")
        }
        else 
        {
           
        }
    }
}